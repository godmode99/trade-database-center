# แผนการพัฒนาโครงการ (ฉบับตั้งต้น)

อัปเดตล่าสุด: 2026-03-03

## ระยะที่ 1: ออกแบบโครงสร้างหลัก (Priority สูง)
1. นิยามโครงสร้างฐานข้อมูลด้วย Prisma
   - ตาราง raw ingestion ต่อแหล่งข้อมูล
   - ตาราง normalized สำหรับใช้งานหน้าเว็บ
   - ตาราง metadata/manifest สำหรับตรวจสอบสถานะงาน
2. นิยาม data contract
   - รูปแบบข้อมูลเข้า/ออกของ pipeline
   - key สำหรับ dedup และ upsert
3. ตั้งค่าพื้นฐานโปรเจคเว็บ
   - Next.js app skeleton
   - DB connection (Neon)
   - Environment management

## ระยะที่ 2: ทำ ingestion pipeline แหล่งหลัก
1. FRED (daily → weekly → monthly)
2. MT5 (เริ่ม daily ก่อน, แล้วเพิ่ม timeframe อื่น)
3. Calendar (weekly snapshot + update)
4. CME FedWatch (probabilities ก่อน quotes)

แนวทางทุก pipeline:
- มี retry + timeout + logging
- มี manifest บอกสถานะ run
- ออกแบบให้รันซ้ำแล้วไม่เสียข้อมูล (idempotent)

## ระยะที่ 3: คุณภาพข้อมูลและการมอนิเตอร์
1. Data validation
   - schema check
   - duplicate check
   - freshness check
2. Alerting
   - แจ้งเตือนเมื่อ fetch ล้มเหลว
   - แจ้งเมื่อข้อมูลผิดปกติ
3. Dashboard สรุปสถานะ ingestion

## ระยะที่ 4: การแสดงผลบนเว็บ
1. หน้า overview ภาพรวมแหล่งข้อมูล
2. หน้า time-series และตารางล่าสุด
3. API ภายในสำหรับ query ตาม source/timeframe

## งานถัดไปทันที (Next Actions)
- [ ] สร้างโครงโปรเจค Next.js + Prisma
- [ ] ออกแบบ Prisma schema v1
- [ ] วางโครง pipeline FRED daily แบบ end-to-end
- [ ] สร้างรายงานสถานะ ingestion แบบง่าย (JSON/หน้าเว็บ)

---

## อัปเดตแผนจากงานรอบล่าสุด
- [x] ออกแบบ file/folder structure ของ `god_project` (เอกสาร + สร้างโฟลเดอร์จริง)
- [ ] สร้าง `db/prisma/schema.prisma` v1
- [ ] วาง shared utility สำหรับ pipeline (`fetch/shared`)
- [ ] ทำ vertical slice แรก `FRED daily -> DB -> Web`

## งานถัดไปทันที (ปรับปรุงล่าสุด)
- [ ] สร้าง Prisma schema v1 (raw + normalized + ingestion manifest)
- [ ] นิยาม data contracts ชุดแรกสำหรับ FRED daily (`data_contracts/raw`, `data_contracts/normalized`)
- [ ] สร้างโครง `fetch/fred` ตาม pattern เดียวกับ ref pipeline
- [ ] เพิ่มหน้า overview เบื้องต้นใน `web_trade-data-hub` สำหรับสถานะ ingestion


## อัปเดตแผนจากงานรอบล่าสุด (Data Management)
- [x] จัดทำเอกสาร `god_project/docs/DATA_MANAGEMENT.md` ครอบคลุมมาตรฐาน data lifecycle
- [ ] แตกเอกสารเป็นสเปกย่อยราย source (FRED/MT5/Calendar/CME)
- [ ] แปลงแนวคิดในเอกสารเป็น `db/prisma/schema.prisma` v1
- [ ] สร้าง data contract ชุดแรกตาม DoD ของ FRED daily

## อัปเดตแผนจากงานรอบล่าสุด (Database Design)
- [x] วิเคราะห์ไฟล์จาก `refจากโปรเจคเก่า/python/Data/raw_data` เพื่อดึงโครงสร้างข้อมูลจริง
- [x] จัดทำ `god_project/docs/DATABASE_DESIGN.md` (v1) สำหรับตาราง ingestion/FRED/Calendar/MT5/CME FedWatch
- [ ] แปลงเอกสารออกแบบเป็น `db/prisma/schema.prisma` v1
- [ ] เขียน migration แรกสำหรับ core tables (`ingestion_runs`, `fred_series`, `fred_observations`, `calendar_events`, `mt5_symbols`, `mt5_ohlcv`, `mt5_features`)
- [ ] เตรียม sample seed/import script จากไฟล์ ref เพื่อทดสอบ upsert + unique constraints

## งานถัดไปทันที (ปรับปรุงล่าสุดจากรอบนี้)
- [ ] สร้าง Prisma models ตาม `god_project/docs/DATABASE_DESIGN.md`
- [ ] นิยาม enum กลาง (source, mode, timeframe, run status)
- [ ] สร้าง data quality checks ขั้นต่ำ (duplicate/freshness/null ratio)
- [x] ตรวจยืนยัน sample CME FedWatch จาก ref และปรับ design ตาม field จริงแล้ว

## อัปเดตแผนจากงานรอบล่าสุด (Review docs)
- [x] ทบทวน `DATABASE_DESIGN.md` และ `DATA_MANAGEMENT.md` เทียบไฟล์จริงจาก ref
- [x] ปรับแบบจำลอง CME FedWatch จาก provisional เป็น field-based ตาม sample จริง
- [x] เพิ่มข้อกำหนดมาตรฐาน ingestion metrics + lineage fields ในเอกสาร
- [ ] แปลงเอกสารล่าสุดเป็น `db/prisma/schema.prisma`
- [ ] สร้าง parser/import test สำหรับ `fedwatch_probabilities.tables` และ `fedwatch_quotes.watchlist`

## งานถัดไปทันที (ปรับปรุงล่าสุดจากรอบ review)
- [ ] สร้าง Prisma models ของ `fedwatch_probabilities` และ `fedwatch_quotes` ตาม field ใหม่
- [ ] นิยาม mapping/type coercion (numeric/text/date/timezone) สำหรับ CME quotes/probabilities
- [ ] เพิ่ม ingestion metrics fields ใน `ingestion_runs` และจุดเขียน manifest
